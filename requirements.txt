# ExpenseAudit AI — pinned runtime dependencies
# Generated from pyproject.toml [dependencies]
# Used by Docker image build (pip install -r requirements.txt)

# Agent framework
google-adk>=0.5.0

# LLM / Gemini SDK (used by ADK internally)
google-generativeai>=0.8.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Config / validation
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# Data generation (needed for cli.py and sample-data scripts)
faker>=30.0.0

# Utilities
python-multipart>=0.0.12
httpx>=0.27.0
